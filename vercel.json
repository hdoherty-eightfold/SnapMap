{
  "version": 2,
  "name": "snapmap",
  "builds": [
    {
      "src": "frontend/package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "dist"
      }
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "https://snapmap-backend.onrender.com/api/$1"
    },
    {
      "src": "/(.*)",
      "dest": "frontend/$1"
    }
  ],
  "build": {
    "env": {
      "VITE_API_URL": "https://snapmap-backend.onrender.com/api"
    }
  }
}
